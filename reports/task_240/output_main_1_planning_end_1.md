Plan is ready for review. It covers three steps:

1. **Create `TaskListEntry` struct + extract `select_ready_task` function** from the inline loop logic
2. **Update `task list`** with compact one-line format, `--json`, and `--select` flags
3. **Add `--json` flag to `task show`** for full Task JSON output