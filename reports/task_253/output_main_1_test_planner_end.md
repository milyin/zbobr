Test plan is complete with 4 checklist items covering the key untested areas:

1. **`sanitize_branch_postfix()` unit tests** — the most critical gap, pure function with edge cases
2. **`repo_short_name()` unit tests** — both backend configs
3. **Simplified `TaskIdentity` tests** — codify the new contract
4. **Preparator removal assertion** — extend existing workflow test