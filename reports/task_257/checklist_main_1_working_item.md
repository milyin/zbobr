Modify `zbobr-api/src/context/mod.rs`: Add `for_prompt: bool` to `MdRecord`, `MdStage`, `MdCompactComment`, `MdContext`. Thread flag through `from_task_context → from_stage_context → from_context_record`. Modify Display impls: MdStage renders `- {stage_name}` only, skip empty stages; MdRecord renders `[ctx_rec_N]` plain; MdCompactComment renders `- user {username}: {text}` no timestamp/link.