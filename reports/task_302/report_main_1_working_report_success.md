## Implementation Summary

All 5 checklist items completed in a single commit (86eb6a44).

### Changes Made

**1. `zbobr-dispatcher/src/cli.rs` — ConfigFileArg & resolve_config_location**
- Changed `ConfigFileArg.path: Option<PathBuf>` to `ConfigFileArg.paths: Vec<PathBuf>`
- Added `-c` short alias alongside `--config` long flag
- Updated `resolve_config_location` to accept `&[PathBuf]` instead of `&Option<PathBuf>`
- Changed `ConfigLocation.config_path` to `ConfigLocation.config_paths: Vec<PathBuf>`
- When paths are empty: falls back to default `zbobr.toml` in cwd (existing behavior)
- When paths are non-empty: canonicalizes each, derives `config_dir` from last file's parent
- Updated CLI hoisting logic (`parse_cli`) to also hoist short flags (e.g. `-c`) that appear after subcommands, not just `--long` flags

**2. `zbobr-macros/src/lib.rs` — merge_toml generation**
- Added `merge_toml_fields` vector alongside existing `merge_fields`
- For nested fields: recursive merge — `(Some(a), Some(b)) => Some(a.merge_toml(b))`, otherwise take whichever side is `Some`
- For leaf fields: `other.field.or(self.field)` — later config wins
- Generated `merge_toml(self, other: Self) -> Self` method on all `*Toml` structs

**3. `zbobr-api/src/config.rs` — WorkflowToml**
- Added manual `merge_toml` method to `WorkflowToml` (which is not generated by the macro)
- Uses same `other.field.or(self.field)` pattern for all 3 fields

**4. `zbobr/src/main.rs` — Config loading flow**
- Updated `resolve_config_location` call to pass `&cli.config_file.paths`
- Replaced single-file loading with fold-merge loop over `location.config_paths`
- Skips non-existent paths (allows default zbobr.toml to be absent)
- Passes merged `Option<RootConfigToml>` to `RootConfig::build()` as before

### Testing
- All 14 zbobr tests pass
- 3 pre-existing failures in zbobr-task-backend-github (CryptoProvider issue, unrelated)
- Build succeeds with no warnings in changed crates