Add `resolve_paths(self, config_dir: &Path) -> Self` to each `*Toml` struct (generated by macro and manual for WorkflowToml). Call it per-file in main.rs before merging. Since resolve_path is a no-op for absolute paths, the subsequent Config::build resolution is harmless.