#Analyze the implementation changes and determine if additional tests are required. Your job is to produce a test plan with list of tests to be added.

- When the context references a detailed record by `ctx_rec_*` ID, use `get_ctx_rec` to fetch the full content before you make decisions or continue your work.


## Workflow

1. Read recent plan and recent implemetation report.
2. Inspect changes in the working branch (e.g., `git diff origin/main...HEAD`) to understand implemented behavior.
3. Decide whether the new feature/bugfix needs additional tests beyond existing coverage. If no new tests are needed, call `report_success` with only a brief rationale and finish.
4. Do NOT propose tests that only assert static prompt text or default config literal values.
5. Treat prompt files and default config examples as source-of-truth authoring artifacts, not behavior contracts to snapshot.
6. Prefer tests that validate behavior and contracts: transitions/routing, parser/serializer invariants, error handling, and externally observable outcomes.
7. Add content-based assertions only when exact text/value stability is itself an explicit product/API contract.
8. Prepare a plan for implementing the required tests as an overview document and set of checklist items
9. Call `add_checklist_item` for each test or group of related tests.
10. Call `report_success` with the overview report test-planning work is complete.

---

# Current task: incorrect timezone of interspersed comments

# Task description

The comment injected into context is displayed with timezone `+0000`. The problem is that comments are generated by task backend which is unaware of timezone setting.
Solution: setup timezone in the TaskBackend::new(), take value from dispatcher config. Use this timezone parameter when creating the comment.

# Destination branch: main

# Work branch: zbobr_fix-245-incorrect-timezone-of-interspersed-comments

# Context

- planning
  - 💬 Plan: add timezone field to backend configs (following `instance` injection patt [ctx_rec_1]
- user milyin: proceed with plan
- planning
  - ✅ Plan approved — 6 checklist items created following the `instance` injection pat [ctx_rec_8]
    - [x] Add `timezone` field to GitHub backend config [ctx_rec_2]
    - [x] Add `timezone` field to FS backend config [ctx_rec_3]
    - [x] Apply timezone conversion in GitHub backend's `get_task_comments_internal()` [ctx_rec_4]
    - [x] Apply timezone conversion in FS backend's `read_comments_structured()` [ctx_rec_5]
    - [x] Inject timezone from dispatcher config at backend construction site [ctx_rec_6]
    - [x] Update test construction sites to set `timezone` field [ctx_rec_7]
- working
  - ✅ All 6 checklist items completed. Added `timezone_offset_seconds: Option<i32>` to [ctx_rec_9]
- reviewing
  - ❌ Review failed: timezone propagation uses raw offset seconds and caller-side inje [ctx_rec_10]
- working
  - ✅ Fixed timezone type in backend configs: replaced Option<i32> with Option<FixedOf [ctx_rec_17]
    - [x] Change timezone_offset_seconds: Option<i32> to timezone: Option<FixedOffsetTz> i [ctx_rec_11]
    - [x] Change timezone_offset_seconds: Option<i32> to timezone: Option<FixedOffsetTz> i [ctx_rec_12]
    - [x] Update GitHub backend to use timezone: Option<FixedOffsetTz> field without recon [ctx_rec_13]
    - [x] Update FS backend to use timezone: Option<FixedOffsetTz> field without reconstru [ctx_rec_14]
    - [x] Update commands.rs to inject timezone field directly from dispatcher_config.time [ctx_rec_15]
    - [x] Update test construction sites to use timezone: None [ctx_rec_16]
- reviewing
  - ✅ Review passed: timezone fix is consistent, complete, and all checklist items are [ctx_rec_18]
