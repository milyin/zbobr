In zbobr-api/src/context/mod.rs, the `MdContext::from_str` parser silently skips malformed `- ...` lines. When a `<!-- stage -->` marker was seen before a `- ...` line, the parse error from `MdStageTitle::from_str` should be propagated (not silently ignored). Track a `after_stage_marker` boolean: set it true when `<!-- stage -->` is encountered (and clear it when a non-empty non-marker line is processed); if it's true and a `- ...` line fails to parse as `MdStageTitle`, return an error.