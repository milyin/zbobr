In zbobr-dispatcher/src/cli.rs, add a `connectivity_failure: bool` field to `SessionOutcome`. Set it `true` only in the `Err(e)` arm of `execute_tool()` (spawn/IO failure, where `execution_output` is None). Keep it `false` for `exit_ok: false` (non-zero exit, which means a task-level failure). Update the exclusion check at line 575 to use `outcome.connectivity_failure` instead of `outcome.execution_error.is_some()`.