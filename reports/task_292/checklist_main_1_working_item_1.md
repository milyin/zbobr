Added `#[arg(conflicts_with = "select")]` to the `task: Option<u64>` field in the `Process` variant of `TaskSubcommand` in `zbobr/src/commands.rs`. Removed the manual `anyhow::bail!("--select and a task ID are mutually exclusive")` runtime check from the handler. The exclusivity is now enforced at parse time by Clap, consistent with the declarative CLI contract pattern used elsewhere.