Run `cargo build` (or `cargo check`) from the repository root to verify the changes compile without errors.

If there are compilation errors, fix them before proceeding.

Also check that any existing tests pass with `cargo test` — particularly any tests that validate stage transitions, role definitions, or configuration loading (if such tests exist).