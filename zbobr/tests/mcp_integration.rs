// This file previously contained combined preparation and planning
// integration tests. Those tests have been split into separate
// `preparation.rs` and `planning.rs` files, each containing their own
// MCP-tester scenario YAML.  The shared helpers remain in
// `mcp_integration_internal`.

mod mcp_integration_internal;

// Intentionally empty; individual tests are defined in dedicated files.
